# Standalone Todo

纯静态待办清单，无需后端。

- 新增任务和备注
- 勾选完成，完成项自动排到底部
- 清除所有已完成任务
- 使用浏览器 `localStorage` 保存数据
- 支持移动端

部署时将此目录内容作为站点根目录，入口文件为 `index.html`。

可使用查询参数预览内置主题，例如：

```text
index.html?theme=macaron
```
